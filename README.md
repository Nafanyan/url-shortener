# URL Shortener

Сервис для сокращения URL-адресов, написанный на Go. Позволяет создавать короткие ссылки для длинных URL и перенаправлять пользователей по этим ссылкам.

## Возможности

- 🔗 Сокращение длинных URL-адресов
- 🎯 Поддержка кастомных алиасов или автоматическая генерация
- 🔐 Базовая HTTP-аутентификация для создания ссылок
- 💾 Хранение данных в SQLite
- 📝 Структурированное логирование
- ⚙️ Гибкая конфигурация через YAML и переменные окружения
- 🚀 Готовность к продакшену с graceful shutdown

## Требования

- Go 1.24.0 или выше
- SQLite3

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd url-shortener
```

2. Установите зависимости:
```bash
go mod download
```

3. Соберите проект:
```bash
go build -o url-shortener ./cmd/url-shortener
```

## Конфигурация

Проект использует конфигурационные файлы YAML. Примеры конфигураций находятся в директории `config/`.

### Переменные окружения

- `CONFIG_PATH` - путь к конфигурационному файлу (обязательно)
- `HTTP_SERVER_PASSWORD` - пароль для HTTP-сервера (может переопределить значение из конфига)

## Запуск

Установите переменную окружения `CONFIG_PATH` и запустите приложение:

```bash
export CONFIG_PATH=./config/local.yaml
./url-shortener
```

Или для продакшена:

```bash
export CONFIG_PATH=./config/prod.yaml
./url-shortener
```

## API

### Создание короткой ссылки

**POST** `/url`

Создает новую короткую ссылку. Требует базовую HTTP-аутентификацию.

**Запрос:**
```json
{
  "url": "https://example.com/very/long/url",
  "alias": "example"  // опционально, если не указан - будет сгенерирован автоматически
}
```

**Ответ:**
```json
{
  "status": "OK",
  "alias": "example"
}
```

**Пример с curl:**
```bash
curl -X POST http://localhost:8082/url \
  -u myuser:mypass \
  -H "Content-Type: application/json" \
  -d '{"url": "https://example.com/very/long/url", "alias": "example"}'
```

### Перенаправление по алиасу

**GET** `/{alias}`

Перенаправляет на оригинальный URL по указанному алиасу.

**Пример:**
```bash
curl -L http://localhost:8082/example
```

## Структура проекта

```
url-shortener/
├── cmd/
│   └── url-shortener/
│       └── main.go              # Точка входа приложения
├── config/
│   ├── local.yaml               # Локальная конфигурация
│   └── prod.yaml                # Продакшен конфигурация
├── internal/
│   ├── config/                  # Пакет конфигурации
│   ├── httpserver/
│   │   ├── handlers/
│   │   │   └── url/
│   │   │       ├── redirect/   # Обработчик редиректов
│   │   │       └── save/        # Обработчик сохранения URL
│   │   └── middleware/
│   │       └── logger/          # Middleware для логирования
│   ├── lib/
│   │   ├── api/                 # API утилиты
│   │   ├── logger/              # Логирование
│   │   └── random/              # Генерация случайных строк
│   └── storage/
│       ├── sqlite/              # SQLite реализация хранилища
│       └── storage.go           # Интерфейс хранилища
├── deployment/
│   └── url-shortener.service    # Systemd service файл
└── tests/                        # Тесты
```

## Разработка

### Запуск тестов

```bash
go test ./...
```

### Генерация моков

Проект использует [mockery](https://github.com/vektra/mockery) для генерации моков:

```bash
go generate ./...
```

## Деплой

### Systemd Service

В директории `deployment/` находится файл `url-shortener.service` для запуска как systemd сервиса.

### GitHub Actions

Проект включает CI/CD pipeline в `.github/workflows/deploy.yaml` для автоматического деплоя.

## Логирование

Приложение использует структурированное логирование через `slog`:

- **local** - текстовый формат с уровнем DEBUG
- **qa** - JSON формат с уровнем DEBUG
- **prod** - JSON формат с уровнем INFO
